# Multi‑Gateway, DNSLink & ENS

- Gateways: w3s.link, dweb.link, cf-ipfs.com — provide quick links
- DNSLink: add TXT `dnslink=/ipfs/<CID>` to domain
- ENS: set `contenthash` to the latest CID
- Automate updates in CI post‑deploy
